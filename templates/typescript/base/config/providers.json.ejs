{
  "repositories": {
    "users": "<%= hasPrisma ? 'auto' : 'in-memory' %>",
    "refreshTokens": "<%= hasPrisma ? 'auto' : 'in-memory' %>"
  },
  "fallback": {
    "users": "in-memory",
    "refreshTokens": "in-memory"
  }
}
