import { createApp } from '../../src/interface/http/app';
<% if (features.includes('auth')) { %>
import { containerReady } from '../../src/infrastructure/container';
<% } %>

export async function buildTestApp() {
<% if (features.includes('auth')) { %>
  await containerReady;
<% } %>
  return createApp();
}
